# SKILL: plan-validate
---
name: plan-validate
version: 1.1.0
granularity: procedural
type: internal
phase: 6
description: Validate execution plan feasibility, completeness, dependency topology, and risk assessment. Ensure plan meets all quality standards before proceeding to execution.
triggers:
  - "validate plan"
  - "review plan"
  - "plan review"
  - "approve plan"
  - "check plan feasibility"
tags:
  - validation
  - review
  - quality-assurance
  - plan-approval
---

## Preconditions
preconditions:
  - plan-develop has been completed
  - execution_plan and task_breakdown have been generated

## Input
input:
  - name: execution_plan
    type: file
    path: "{{SPEC_DIR}}/projects/{{project_name}}/plan.md"
    description: Execution plan document

  - name: task_breakdown
    type: data
    description: Task breakdown (from {{SPEC_DIR}}/projects/{{project_name}}/tasks/)

  - name: project_md
    type: file
    path: "{{SPEC_DIR}}/projects/{{project_name}}/SPEC.md"
    description: Project analysis document

## Output
output:
  - name: review_result
    type: enum
    enum_values: [approved, rejected, needs_revision]
    description: Review outcome

  - name: review_comments
    type: data[]
    description: List of review comments with severity and type
    items:
      - type: enum
        severity: enum
        message: string
        task_id: string

  - name: revised_plan
    type: file
    path: "{{SPEC_DIR}}/projects/{{project_name}}/plan-revised.md"
    description: Revised plan (if any)
    required: false

  - name: validation_summary
    type: object
    description: Summary of validation results
    properties:
      dependencies_valid: boolean
      risks_mitigated: boolean
      completeness_score: float
      estimated_duration_valid: boolean

## Steps
steps:
  - id: check-feasibility
    description: Check technical and resource feasibility
    type: internal
    continue_on_error: false
    timeout: 5m

  - id: check-completeness
    description: Verify all tasks and acceptance criteria are covered
    type: internal
    continue_on_error: false
    timeout: 5m

  - id: validate-dependencies
    description: Validate dependency topology for circular dependencies
    type: internal
    continue_on_error: true
    timeout: 5m

  - id: review-risks
    description: Review risk assessment and mitigation measures
    type: internal
    continue_on_error: false
    timeout: 3m

  - id: generate-review-result
    description: Generate final review result
    type: internal
    continue_on_error: false
    timeout: 2m

## Checkpoint
checkpoint:
  required: true
  message: "Plan review complete. Result: {review_result}. Found {issue_count} issues ({error_count} errors, {warning_count} warnings). Review comments: {comments_summary}. Please confirm approval or request revisions."

## Hook Configuration
hooks:
  on-approve:
    - trigger: on-plan-approved
      action: auto-trigger-next-skill
      next_skill: task-execute
  on-reject:
    - trigger: on-plan-rejected
      action: notify-master
      message: "Plan rejected: {reason}"
      fallback: return-to-plan-develop
  on-revision-needed:
    - trigger: on-revision-needed
      action: notify-master
      message: "Plan needs revision: {issues_summary}"

---

# INSTRUCTIONS

You are a plan review expert. Verify whether the execution plan generated by plan-develop is feasible.

## Review Criteria

### 1. Feasibility Review

```
Review items:
├── Technical feasibility: Are there any technical blockers?
├── Resource feasibility: Are there sufficient personnel and time?
├── Dependency feasibility: Are dependencies correct?
└── Risk controllability: Are risks acceptable?
```

### 2. Completeness Review

```
Review items:
├── Are all tasks covered?
├── Are acceptance criteria clear?
├── Have risks been assessed?
└── Are there any missed edge cases?
```

### 3. Dependency Topology Validation

```
Validation rules:
1. No circular dependencies
2. All dependencies have clear prerequisite tasks
3. Dependency chain depth does not exceed 10 levels
4. Modifications to the same file are in the same task

Circular dependency detection:
A → B → C → A  ← Reject

Valid example:
A → B → C → D  ← Pass
```

### 4. Risk Review

```
Review items:
├── Do high-risk tasks have mitigation measures?
├── Do tasks on the critical path have alternatives?
└── Is the time buffer sufficient?
```

## Error Handling

| Error Type | Handling Strategy | Recovery Action |
|------------|-----------------|----------------|
| Circular dependency | Hard reject | Return plan for revision |
| Missing acceptance criteria | Needs revision | Add missing criteria |
| Unmitigated high risk | Needs revision | Request risk mitigation |
| Incomplete task definition | Needs revision | Request task completion |
| Missing task dependencies | Auto-fix if possible | Add inferred dependencies |

### Error Recovery Scenarios

1. **Circular dependency detected**: Log specific tasks; reject plan with guidance
2. **Missing acceptance criteria**: Suggest criteria based on task type
3. **Unrealistic timeline**: Calculate realistic estimate; flag for user decision
4. **Inconsistent agent assignments**: Suggest corrections; allow override

## Review Results

### approved

All review items passed. The plan is feasible.

```
review_result: approved
review_comments:
  - { type: "info", message: "Dependency topology is valid" }
  - { type: "info", message: "Risk assessment is complete" }
  - { type: "info", message: "All acceptance criteria are defined" }
validation_summary:
  dependencies_valid: true
  risks_mitigated: true
  completeness_score: 100
  estimated_duration_valid: true
```

### rejected

Serious issues exist. The plan is not feasible.

```
review_result: rejected
review_comments:
  - { type: "error", message: "Circular dependency found: task-003 → task-005 → task-003" }
  - { type: "error", message: "High-risk tasks lack mitigation measures" }
validation_summary:
  dependencies_valid: false
  risks_mitigated: false
  completeness_score: 65
  estimated_duration_valid: true
```

### needs_revision

Fixable issues exist. Revision required before re-review.

```
review_result: needs_revision
review_comments:
  - { type: "warning", message: "task-007 scope is too broad, consider splitting" }
  - { type: "suggestion", message: "Add Phase 0 for environment preparation" }
validation_summary:
  dependencies_valid: true
  risks_mitigated: true
  completeness_score: 85
  estimated_duration_valid: false
```

## Revision Process

When review_result is needs_revision or rejected:

```
1. Read review_comments
2. Revise the plan for each issue
3. Generate plan-revised.md after revision
4. Re-review (until approved)
```

## Key Constraints

1. **Circular dependencies must be rejected**: This is a hard requirement
2. **High risks must be mitigated**: Concrete measures are required
3. **Reviews must be transparent**: All issues must be recorded in comments
4. **Revisions must be complete**: All issues must be resolved